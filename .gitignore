# Credenciales reales del envío de correo (NO subir al repositorio)
send.config.php

# Datos en runtime del servidor (límite de peticiones, logs, etc.)
/data/
error_log
*.log

# Carpeta manual en el servidor (Nextcloud) para intercambio de archivos.
# No forma parte del sitio y contiene credenciales reales en config/config.php:
# NUNCA debe subirse al repositorio.
/share/

# Sistema
.DS_Store
Thumbs.db
